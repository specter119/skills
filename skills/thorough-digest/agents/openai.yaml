interface:
  display_name: "Thorough Digest"
  short_description: "Exhaustively digest local materials in parallel"
  default_prompt: "Use $thorough-digest to batch process many local files or materials with parallel sub-agents when nothing should be skipped."
